function primeiraArray(array)
let novaArray = array.slice().sort((a,b) => a-b);
return novaArray


para criar uma nova array apartir de outra, primeiro tenho q usar o slice para fazer uma cópia.
só depois posso usar o sort para ordenar.
o a e o b não percebi.
