//Cria uma função que recebe dois inteiros como argumentos e calcula o resultado de elevar o primeiro ao segundo. usando bighint.
function umElevadoAoOutro(um, outro) {

  const base = BigInt(um);
  const expoente = BigInt(outro);

  const resultado = base ** expoente;

  return resultado;
}
